### Install docker

### Start docker
* `docker-compose up -d`

### Stop docker
* `docker-compose down`

### Set access key
* `echo ${method}:${password}@${address}:${port} | base64`
* `ss://access-key`